name: Validate

on:
  push:
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:
  hassfest:
    name: Hassfest
    uses: home-assistant/actions/hassfest@master

  hacs:
    name: HACS
    uses: hacs/action@main
    with:
      category: integration
